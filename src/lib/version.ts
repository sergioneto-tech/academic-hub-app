// Versão da aplicação (incrementar quando publicares uma atualização)
// Nota: é uma constante simples para ficar compatível com qualquer pipeline de deploy.
export const APP_VERSION = "0.2.0";

// Versão do esquema de dados (muda apenas quando o formato do estado persistido muda)
export const SCHEMA_VERSION = 4;
