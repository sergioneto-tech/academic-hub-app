import { createClient } from "npm:@supabase/supabase-js@2.95.3";
type EventType="login_success"|"login_failed"|"password_recovery"|"rate_limited";type Body={eventType?:EventType;email?:string;deviceLabel?:string;appVersion?:string};const allowed=new Set<EventType>(["login_success","login_failed","password_recovery","rate_limited"]);const json=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{"Content-Type":"application/json","Cache-Control":"no-store"}});function requestIp(req:Request){const f=req.headers.get("x-forwarded-for")?.split(",")[0]?.trim();return req.headers.get("cf-connecting-ip")||req.headers.get("x-real-ip")||f||null;}
export default{async fetch(req:Request){if(req.method!=="POST")return json({error:"Method not allowed"},405);const origin=req.headers.get("origin");if(origin&&origin!=="https://academichub.sergioneto.pt"&&!origin.startsWith("http://localhost"))return json({error:"Forbidden"},403);const url=Deno.env.get("SUPABASE_URL"),key=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!url||!key)return json({error:"Configuration unavailable"},503);let body:Body;try{body=await req.json()}catch{return json({error:"Invalid body"},400)}const eventType=body.eventType;if(!eventType||!allowed.has(eventType))return json({error:"Invalid event"},400);const db=createClient(url,key,{auth:{persistSession:false}});let userId:string|null=null;let confidence="medium";const auth=req.headers.get("authorization")||"";const token=auth.toLowerCase().startsWith("bearer ")?auth.slice(7).trim():"";if(eventType==="login_success"){if(!token)return json({error:"Unauthorized"},401);const{data,error}=await db.auth.getUser(token);if(error||!data.user?.id)return json({error:"Unauthorized"},401);userId=data.user.id;confidence="high";}else if(body.email?.trim()){const{data}=await db.rpc("security_resolve_user_target",{p_email:body.email.trim().toLowerCase()});const row=Array.isArray(data)?data[0]:data;userId=row?.user_id??null;}const ip=requestIp(req);const country=(req.headers.get("cf-ipcountry")||"").trim().toUpperCase();const ua=(req.headers.get("user-agent")||"").slice(0,512);
if(userId&&ip&&eventType!=="login_success"){await db.rpc("security_record_auth_hint",{p_user_id:userId,p_event_type:eventType,p_ip_address:ip,p_metadata:{channel:"app-auth-correlation"}});return json({ok:true,correlationOnly:true});}
const severity=eventType==="rate_limited"?2:1;const{error}=await db.rpc("security_ingest_event",{p_event_type:eventType,p_user_id:userId,p_source:"academic_hub_client",p_confidence:confidence,p_severity:severity,p_ip_address:ip,p_country_code:country||null,p_user_agent:ua||null,p_device_label:body.deviceLabel?.slice(0,128)||null,p_app_version:body.appVersion?.slice(0,32)||null,p_metadata:{channel:"app-auth-telemetry"}});if(error){console.error("security-event ingest",error);return json({error:"Event unavailable"},503)}return json({ok:true});}};
